apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: inventory

spec:
  type: LoadBalancer
  selector:
    app: inventory-api
  ports:
    - name: http
      port: 80
      targetPort: 8080